{
  "name": "trystack-cli",
  "version": "0.0.2",
  "private": true,
  "description": "TryStack CLI: resolve repo -> recipe -> docker compose up",
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  },
  "bin": {
    "trystack-cli": "entry.js"
  },
  "dependencies": {
    "yaml": "^2.8.0"
  }
}

