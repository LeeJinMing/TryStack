# transmission (default)

## What you get

- **Try locally**: Transmission Web UI at `http://localhost:9091`.
- **Local storage**: Separate volumes for config and downloads.

## Run

In this directory:

```bash
docker compose up -d
```

Then open `http://localhost:9091` in your browser.

## Stop

```bash
docker compose down -v
```

