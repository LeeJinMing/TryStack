# uptime-kuma (default)

## What you get

- **Try locally**: 启动后访问 `http://localhost:3001` 打开 UI。
- **No external keys**: 基础体验不需要第三方 API key。

## Run

在本目录执行：

```bash
docker compose up -d
```

打开 `http://localhost:3001`，按引导创建管理员账号。

## Stop

```bash
docker compose down -v
```
