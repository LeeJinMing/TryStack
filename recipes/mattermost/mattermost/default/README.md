# mattermost/mattermost (default)

Runs **Mattermost Team Edition** locally with a bundled **Postgres** database.

## Open

- UI: `http://localhost:8065`

On first run, complete the admin setup in the browser.

## Notes

- This is intended for **local evaluation**. For production deployments, refer to upstream documentation.
