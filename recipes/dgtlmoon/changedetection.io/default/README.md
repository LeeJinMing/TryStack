# dgtlmoon/changedetection.io (default)

## What you get

- **Try locally**: After startup, open `http://localhost:5003` in your browser.
- **No external keys**: No third-party API keys required.

## Run

In this directory:

```bash
docker compose up -d
```

## Stop

```bash
docker compose down -v
```
