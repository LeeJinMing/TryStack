# MicroBin

Lightweight, self-hosted pastebin (text + file uploads).

## Quick start

```bash
npx --yes -p github:LeeJinMing/TryStack#v0.0.1 trystack up szabodanika/microbin
```

Open:

- `http://localhost:8102`

## Notes

- This is intended for local evaluation (Try locally).
