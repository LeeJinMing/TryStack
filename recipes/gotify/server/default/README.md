# Gotify

Self-hosted notifications server with a web UI.

## Quick start

```bash
npx --yes -p github:LeeJinMing/TryStack#v0.0.1 trystack up gotify/server
```

Open:

- `http://localhost:8105`

## Notes

- First run uses default credentials shown on the login page.
