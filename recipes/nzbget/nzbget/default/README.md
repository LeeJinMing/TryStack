# nzbget (default)

## What you get

- **Try locally**: NZBGet Web UI at `http://localhost:6789`.
- **Local storage**: Separate volumes for config and downloads.

## Run

In this directory:

```bash
docker compose up -d
```

Then open `http://localhost:6789` in your browser.

## Stop

```bash
docker compose down -v
```

