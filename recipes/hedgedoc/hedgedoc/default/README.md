# hedgedoc (default)

## What you get

- **Try locally**: A collaborative Markdown editor at `http://localhost:3004`.
- **Local Postgres**: Uses a bundled Postgres container for storage.

## Run

In this directory:

```bash
docker compose up -d
```

Then open `http://localhost:3004` in your browser.

## Stop

```bash
docker compose down -v
```

