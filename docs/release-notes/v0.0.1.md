# v0.0.1

## Highlights

- GitHub Pages Portal deployment (static build).
- Portal commands are pinned to `v0.0.1` for reproducible runs.
- CLI: add `scaffold` command for generating recipe skeletons.
- CI: verify-recipes runs without Docker (schema + file validation).

## Notes

- This is an MVP release focused on “Try locally” via recipes + Portal + CLI.
